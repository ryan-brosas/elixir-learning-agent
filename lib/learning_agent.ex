defmodule LearningAgent do
  @moduledoc """
  Standalone Elixir repository-learning agent.

  Milestone 1 scope: pure, deterministic domain model with no external side effects.
  The model owns no durable state and no telemetry here; it is a provider-neutral value
  layer that scheduling, SQL, MCP, and provider adapters (added in later milestones) will
  build on. Source code and direct tests remain authoritative.
  """
end
